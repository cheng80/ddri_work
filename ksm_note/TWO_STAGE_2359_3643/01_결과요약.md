# 2359·3643 2단계 튜닝 결과 요약

## 1) 이번에 한 작업
- `2359`, `3643`만 분리 학습
- 2단계 모델 적용
  - 1단계: `0대 vs 1대 이상` 분류
  - 2단계: `1대 이상일 때` 건수 회귀
- `2024(valid)`에서 임계값/스케일/바이어스 튜닝 후 `2025(test)` 평가

## 2) 핵심 결과 (Test 2025)

| 대여소 | 기존 단일모델 Exact | 2단계 Exact | 변화 |
|---|---:|---:|---:|
| 2359 | 0.5209 | 0.5747 | +0.0538 |
| 3643 | 0.5798 | 0.6712 | +0.0914 |

정리:
- Exact(정수 반올림 일치율)는 두 대여소 모두 개선됨.
- 특히 `3643`은 `0.67`까지 올라와서 목표 `0.7`에 근접.
- 다만 RMSE/±1 정확도는 일부 하락해, “정확일치 최적화”에 따른 트레이드오프가 존재.

## 3) 최적 튜닝 파라미터
- 2359: `threshold=0.84`, `scale=0.74`, `bias=-0.35`
- 3643: `threshold=0.82`, `scale=0.70`, `bias=-0.40`

## 4) 0.7~0.8로 더 올리려면
1. 지표를 Exact 하나로만 두지 말고 `Exact + ±1 + Bucket` 가중 목적함수로 튜닝
2. 시간대별 파라미터 분리(출근/주간/야간)로 임계값 다르게 적용
3. 실시간에 가까운 `재고/인접대여소 재고/유출입` feature 추가
4. 3단계 분류(0 / 1~2 / 3+) 후 세부 회귀하는 하이브리드 구조 적용

## 5) 결과물 위치
- 요약 CSV: `two_stage_top2_summary.csv`
- 대여소별 결과: `station_2359_*`, `station_3643_*`
- 시각화: `images/*.png`
- 실행 코드: `run_two_stage_top2.py`
